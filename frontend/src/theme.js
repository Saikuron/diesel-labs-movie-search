import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#879da3',
    },
    secondary: {
      main: '#a38d87',
    },
    background: {
      default: '#ebeeef',
    },
  },
});

export default theme;